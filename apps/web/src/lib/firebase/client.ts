import{getApp,getApps,initializeApp,type FirebaseOptions}from"firebase/app";import{getAuth,inMemoryPersistence,setPersistence}from"firebase/auth";
let persistence:Promise<void>|undefined;
export function firebaseClientAuth(){const apiKey=process.env.NEXT_PUBLIC_FIREBASE_API_KEY,projectId=process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID;if(!apiKey||!projectId)throw new Error("Firebase client configuration is missing.");const config:FirebaseOptions={apiKey,projectId,...(process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN?{authDomain:process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}:{}),...(process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET?{storageBucket:process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}:{}),...(process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID?{messagingSenderId:process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}:{}),...(process.env.NEXT_PUBLIC_FIREBASE_APP_ID?{appId:process.env.NEXT_PUBLIC_FIREBASE_APP_ID}:{})};const app=getApps().length?getApp():initializeApp(config);const auth=getAuth(app);persistence??=setPersistence(auth,inMemoryPersistence);return{auth,ready:persistence}}
