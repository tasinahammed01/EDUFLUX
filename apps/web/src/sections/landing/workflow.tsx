import { ChartNoAxesCombined, FileCheck2, NotebookPen, ScanText, Send, Sparkles } from "lucide-react";
const stages=[[NotebookPen,"Plan","Begin with the learning goal, not the software."],[Sparkles,"Create","Build a purposeful task with support where it matters."],[Send,"Submit","Let students respond in the format that fits their thinking."],[ScanText,"Understand","Surface patterns across typed and handwritten work."],[FileCheck2,"Review","Keep teacher judgement at the centre of every response."],[ChartNoAxesCombined,"Improve","Make the next step visible—to you and to the student."]];
export function Workflow(){return <section id="workflow" className="workflow-section"><div className="workflow-heading"><p className="section-kicker">The teaching workflow</p><h2>One continuous line<br/><em>from intent to insight.</em></h2><p>Scroll to follow the work.</p></div><div className="workflow-scenes">{stages.map(([Icon,title,copy],index)=>{const Mark=Icon as typeof NotebookPen;return <article className="workflow-scene" key={title as string}><span>0{index+1}</span><div className="workflow-visual"><Mark aria-hidden="true"/><i/><i/><i/></div><div><h3>{title as string}</h3><p>{copy as string}</p></div></article>})}</div></section>}
